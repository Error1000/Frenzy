Translations from C to ASM:

if(--B == 0) goto X;
```
		FROMA ; Save A
		{regA}
		CHNGA 0x3 ; A = 0, for for the JMPZ
		CHNGB 0x0 ; Sub 1 from b, updating flags register
		TOA       ; Restore a
		{regA}
		JMPZ      ; JMP if zero to X
		{X}
```